const Data = ({fullname, profession}) => {
    return(
        <div>
            <p style = {{color : 'blue'}}>{fullname}</p>
            <p>{profession}</p>
        </div>
    ) 
}

export default Data;